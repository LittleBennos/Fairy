# Security Update Requirements - December 2024
# This file contains security-patched versions of all dependencies

# Core Django - Security Patched
Django==4.2.24  # Fixes CVE issues with SQL injection, DoS, and log injection
djangorestframework==3.15.2  # Fixes XSS vulnerability in break_long_headers
django-cors-headers==4.9.0
django-filter==25.1

# Database
psycopg2-binary==2.9.10  # Latest stable

# Authentication - Security Patched
djangorestframework-simplejwt==5.5.1  # Fixes authentication bypass vulnerability
django-allauth==65.3.0  # Latest stable

# Environment variables
python-decouple==3.8

# Redis cache
redis==5.2.1  # Latest stable
django-redis==5.4.0

# API documentation
drf-spectacular==0.28.0  # Latest stable

# Utilities - Updated for security
python-dateutil==2.9.0.post0  # Latest stable
pytz==2024.2  # Latest timezone data

# Production server
gunicorn==23.0.0  # For production deployment
whitenoise==6.8.2  # For static file serving

# Security monitoring (production only)
sentry-sdk==2.19.3  # For error tracking

# Note: django-debug-toolbar removed (development only)